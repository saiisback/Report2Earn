# Report2Earn Smart Contract Wrapper

This directory now proxies to the shared `@r2e/smart-contracts` package located in `apps/smart-contracts`.

## Quick Commands

```bash
# install deps (installs the shared package in linked mode)
npm install

# deploy helper scripts (delegates to shared package)
npm run deploy:creator
npm run deploy
```
